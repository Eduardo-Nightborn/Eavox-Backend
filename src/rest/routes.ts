import { Router } from 'express';
import { Usecases } from '../usecases';

export const initRoutes = (app: Router, usecases: Usecases) => {
  // const exampleRouter = initExampleRoutes(usecases);
  // app.use('/example', exampleRouter);
};
